application:
  name: test-app
  version: 1.0.0
  environment: development

database:
  host: localhost
  port: 5432
  name: testdb

features:
  dark_mode: true
  notifications: enabled
  max_retries: 3

logging:
  level: info
  format: json
  output: stdout
